@{
    //The razor view start file tells Razor to use a layout file in the HTML that it generates
    // this reduces code duplication in views. We need to create the shared view in Views/Shared/_Layout.cshtml
    Layout = "_Layout";
}