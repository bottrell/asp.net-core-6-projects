@model IQueryable<Product>

@foreach (var p in Model ?? Enumerable.Empty<Product>()) {
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <!-- ToString("c") converts to the currency of the language of the project -->
        <h4>@p.Price.ToString("c")</h4>
    </div>    
}